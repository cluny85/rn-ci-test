import { Component } from 'react';
import { AddonStore } from '@storybook/addons';
import { API } from '@storybook/api';
import { StoryStore } from '@storybook/client-api';
export declare type Channel = ReturnType<AddonStore['getChannel']>;
declare type Selection = ReturnType<StoryStore['fromId']>;
interface BackgroundPanelProps {
    channel: Channel;
    api: API;
    active: boolean;
}
interface BackgroundPanelState {
    selection: Selection;
}
export default class BackgroundPanel extends Component<BackgroundPanelProps, BackgroundPanelState> {
    componentDidMount(): void;
    componentWillUnmount(): void;
    setBackgroundFromSwatch: (background: string) => void;
    onStorySelected: (selection: import("@storybook/client-api/dist/types").StoreItem) => void;
    render(): JSX.Element;
}
export {};
